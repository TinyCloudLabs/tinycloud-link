{
  "name": "@tinycloudlabs/tinycloud-link-front",
  "version": "0.1.0",
  "description": "Cloudflare Worker front for *.tinycloud.link: terminates the wildcard route dstack-ingress can't, and forwards to the tinycloud-link relay with the original host carried in X-Forwarded-Host",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "^5.0.0",
    "wrangler": "^4.112.0"
  }
}
